<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.remasteragent</groupId>
    <artifactId>remaster-agent-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>remaster-agent-web</artifactId>
  <name>remaster-agent-web</name>
  <description>API 进程：REST + SSE 推送 + 鉴权。不执行任何长任务，只写状态与投递队列</description>

  <dependencies>
    <dependency>
      <groupId>com.remasteragent</groupId>
      <artifactId>remaster-agent-common</artifactId>
    </dependency>
    <dependency>
      <groupId>com.remasteragent</groupId>
      <artifactId>remaster-agent-core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.remasteragent</groupId>
      <artifactId>remaster-agent-tools</artifactId>
    </dependency>
    <dependency>
      <groupId>com.remasteragent</groupId>
      <artifactId>remaster-agent-llm</artifactId>
    </dependency>

    <!-- 选 Spring MVC + SseEmitter 而非 WebFlux，理由见 docs/ARCHITECTURE.md ADR-2 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!-- 演示模式登录：仅保护 /api/demo/**，其余端点保持原样（避免破坏双进程/评测 harness） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.remasteragent.web.WebApplication</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
