# ============================================================
# 密钥与本地配置 —— 绝对不要提交。仓库公开，泄露即等于泄露生产凭据
# ============================================================
.env
.env.*
!.env.example
**/application-local.yml
**/application-local.yaml
**/application-secret.yml
**/application-secret.yaml

# ============================================================
# Java / Maven
# ============================================================
target/
*.class
*.jar
*.war
*.ear
dependency-reduced-pom.xml
*.versionsBackup
.mvn/wrapper/maven-wrapper.jar
.mvn/wrapper/maven-wrapper.properties

# ============================================================
# 运行时产物
# ============================================================
logs/
*.log
*.exec
/data/
/sandbox-workspace/
generated-sources/

# 沙箱工作目录：每个任务一份被测工程副本，纯运行时产物
.remaster-workspaces/

# ============================================================
# 前端 AgentVue
# ============================================================
node_modules/
# 锁文件不入库：它由本地 npm 版本解析生成，跨机器/跨版本差异极大，
# 提交它只会制造无意义的巨型 diff 与合并冲突。依赖版本由 package.json 的语义化范围约束。
package-lock.json
dist/
dist-ssr/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============================================================
# IDE
# ============================================================
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/extensions.json
.settings/
.classpath
.project
.factorypath

# ============================================================
# 操作系统
# ============================================================
.DS_Store
Thumbs.db
desktop.ini

# ============================================================
# 工作区工具数据（AI 助手的记忆与本地 skill，属本机产物）
# ============================================================
.workbuddy/

# ============================================================
# 文档与操作手册（本地留档，不进 GitHub）
# ============================================================
docs/
AGENTS.md

# ============================================================
# 评测产物（runs.json / report.md，每次跑都不同，且含本机绝对路径）
# ============================================================
eval-results/
